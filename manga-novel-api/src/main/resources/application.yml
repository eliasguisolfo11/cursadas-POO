server:
  port: 8080

spring:
  application:
    name: manga-novela-api

data:
  file:
    path: ${DATA_PATH:./data/}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /api/docs

logging:
  level:
    com.example.manga: INFO
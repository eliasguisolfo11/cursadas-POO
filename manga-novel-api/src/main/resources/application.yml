server:
  port: 8080

spring:
  application:
    name: manga-novela-api

data:
  file:
    path: data/

cli:
  enabled: false

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /api/docs
    operationsSorter: method

logging:
  level:
    com.example.manga: INFO
# .env — variables de entorno locales (NO versionar valores reales en producción)
APP_NAME=boilerplate
APP_ENV=development
APP_PORT=3000

# Configuración de base de datos (ejemplo)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=boilerplate
DB_USER=root
DB_PASSWORD=secret

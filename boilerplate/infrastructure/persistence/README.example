# PERSISTENCIA — implementación concreta de los puertos (ej. users/ports/UserRepository).
# Aquí van ORM, esquemas, migraciones. Implementa la interfaz, NO expone detalles.

# class SqlUserRepository implements UserRepository:
#   method save(user)     -> INSERT/UPDATE ...
#   method findById(id)   -> SELECT ...
#   method findByEmail(e) -> SELECT ...

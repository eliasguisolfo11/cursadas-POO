# .env.example — plantilla de variables de entorno (SÍ versionar; sin secretos)
# Copia este archivo a .env y completa los valores reales. Nunca commits .env con secretos.

APP_NAME=
APP_ENV=development
APP_PORT=3000

# Configuración de base de datos (ejemplo)
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

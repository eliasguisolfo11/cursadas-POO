# KERNEL — primitivas de dominio transversales (base para todo el monolito).

value-object Id:        # value object genérico
  value : string

class DomainEvent:      # base para eventos de dominio
  occurredAt : DateTime

class Entity<TId>:      # base para entidades
  id : TId
